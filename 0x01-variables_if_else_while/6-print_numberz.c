#include <stdio.h>

/**
 * main - for loop digit
 * without char
 * Return: (Success)
 */

int main(void)

{
	int dio;

	for (dio = '0'; dio <= '9'; dio++)
	putchar(dio);
	putchar('\n');
	return (0);
}
